<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.TravelActivity" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
    </style>

    <style name="Theme.TravelActivity" parent="Base.Theme.TravelActivity" />
    <style name="ShowActionBar" parent="Theme.Material3.DayNight"/>
    <style name="Theme.SplashScreen" parent="Base.Theme.TravelActivity" />
    <style name="SearchView" parent=""/>
</resources>